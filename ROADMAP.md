# Revive 开发路线图

这份文档用于记录：
- 当前已经完成的内容
- 接下来按顺序要做的事情
- 后台表的人话解释
- 开发时需要特别注意的点

如果后面会话切换，这份文档可以作为继续推进的依据。

## 1. 当前已经完成的内容

- 首页已经从“大文件堆逻辑”开始拆分，新增了 `lib/collections`、`lib/metadata`、`components/collection`。
- 已新增统一 metadata 接口：`/api/metadata`。
- metadata 解析已经支持一套基础来源判断。
- 已新增阶段一数据库草案文件：`supabase/phase1_schema.sql`。
- 首页已经接入新的 metadata 解析链路，不再只靠原来的前端正则。

## 2. 当前推荐开发顺序

### 当前加载方案说明

当前首页先采用“方案 2”：
- 页面框架先显示
- 收藏列表作为局部区域单独加载
- 不再把鉴权和列表查询绑成整页阻塞的 `加载中...`

这样做的原因：
- 比原来的整页阻塞加载体验明显更好
- 改动量比完整的服务端首屏预取更小
- 同时也给后面升级到“方案 3”铺路

后续如果继续优化，可以平滑升级到“方案 3”：
- 服务端完成更多首屏数据准备
- 引入 `loading.tsx` / Suspense
- 客户端主要负责交互和局部刷新

也就是说，当前方案不是临时补丁，而是一个适合继续演进的中间形态。

### 阶段一：把收藏底座做稳

1. 执行 `supabase/phase1_schema.sql`
2. 接上 `categories`
3. 做基础筛选
4. 记录打开行为，例如 `last_opened_at`
5. 在卡片上预留提醒入口

这一步的目标不是“功能很多”，而是：
- 收藏真正能整理
- 页面结构稳定
- 后面接提醒不会返工

### 阶段二：做 Telegram 单条提醒

1. 对单条收藏设置提醒时间
2. 卡片上显示“已设提醒”
3. 支持修改提醒
4. 支持取消提醒
5. 到点后 Telegram 发送提醒

当前阶段暂不做：
- 每日摘要提醒
- 关键词提醒
- 重复提醒

### 阶段三：做整理增强

1. 标签 `tags`
2. 搜索
3. 排序
4. 更好的筛选组合

### 阶段四：做回顾和浏览能力

1. 浏览记录
2. 最近打开
3. 打开次数统计
4. 时间维度回顾
5. 后续再考虑底部导航

### 阶段五：个人展示页（后期）

目标：
- 允许用户把私藏中的部分内容整理成一个可分享的公开主页

第一版已确认方向：
- 每个用户一个公开主页
- 主路由采用 `/u/[slug]`
- `slug` 由用户手动设置，且必须唯一
- 公开页内容采用“手动精选条目”
- 展示内容复用原收藏标题
- 每条展示项可选填写一句简介
- 每条展示项支持：
  - 排序
  - 显示 / 隐藏
  - 简介
- 公开页包含：
  - 昵称
  - 一句简介
- 管理入口采用单独的“展示页管理”
- 不在收藏卡片上直接做“公开 / 取消公开”

后期建议新增的数据层：
- `public_profiles`
- `showcase_items`

注意：
- 公开展示层和私有收藏层必须分离
- 不允许直接把 `collections` 当成公开页数据源

## 3. 下一批待做清单

### 3.1 数据库

- 在 Supabase 执行 `supabase/phase1_schema.sql`
- 补 RLS 和 policy
- 确认现有 `collections` 表与新字段兼容
- 检查 `categories`、`item_reminders`、`user_telegram_connections` 是否创建成功

### 3.2 分类

- 新增 `lib/categories/types.ts`
- 新增 `lib/categories/queries.ts`
- 新增 `lib/categories/mutations.ts`
- 首页加载用户分类
- 收藏支持选择分类
- 列表支持按分类筛选

### 3.3 基础筛选

- 新增 `components/collection/CollectionFilters.tsx`
- 支持按平台筛选
- 支持按分类筛选
- 预留后续扩展搜索和标签的位置

### 3.4 打开记录

- 用户点击“打开”时更新 `last_opened_at`
- 后续可再补 `opened_count`
- 不需要把“打开过”做成强制状态 UI

### 3.5 提醒入口预留

- 卡片右上角做 `...` 菜单
- 菜单里未来放：
  - `设提醒`
  - `编辑标题`
  - `删除`

当前阶段可以先做菜单结构和占位，不一定一次接完 Telegram。

## 4. 后台表的人话解释

### `collections`

这是主表，一行就是一条收藏内容。

用途：
- 存标题
- 存链接
- 存来源平台
- 存原始粘贴内容
- 存 metadata 结果
- 以后还能挂分类、提醒、浏览记录

### `categories`

这是用户自定义分类表。

一行就是一个分类，比如：
- 健身
- 英语
- 灵感

规则：
- 主键是 `id`
- 同一用户下，`name` 唯一
- 也就是 `unique (user_id, name)`

### `user_telegram_connections`

这是 Telegram 绑定表。

作用：
- 让系统知道“这个站内用户”对应 Telegram 上哪个 `chat_id`

没有这张表，系统就不知道消息发给谁。

### `item_reminders`

这是单条内容提醒表。

一行就是一次提醒：
- 哪个用户
- 哪条收藏
- 什么时候提醒

第一版规则：
- 一条收藏只允许 1 个有效提醒

## 5. metadata 字段说明

### `metadata_source`

表示标题是从哪里来的。

这些值不是网站自动给的字段，而是我们自己定义的一组“来源标签”。

### `metadata_confidence`

表示系统对当前标题结果的可信度。

它不是抓来的，是后端根据规则打的“信心分”。

用途：
- 判断哪些标题更可信
- 判断哪些内容需要用户手动修正
- 帮助以后优化 metadata 解析

## 6. 开发期间必须注意的事

### 6.1 不要把提醒逻辑塞回收藏主表

提醒是独立模块，不要把这些概念混进 `collections`：
- 重复提醒规则
- 提醒时间列表
- 提醒发送状态

它们应该留在 `item_reminders` 或后续提醒模块里。

### 6.2 不要把分类和平台混在一起

平台是系统字段，分类是用户字段。  
后面不允许出现“拿平台替代分类”的实现。

### 6.3 不要过早做太多空目录和空模块

当前只做真正要用到的目录：
- `lib/collections`
- `lib/metadata`
- `lib/categories`
- `lib/reminders`
- `lib/telegram`
- `components/collection`

先不要提前铺太多：
- `app/review`
- `app/search`
- `app/settings`
- `components/navigation`

### 6.4 不要把 metadata 规则重新写回前端

metadata 解析必须保持一套后端规则。  
前端只消费结果，不重复实现。

### 6.5 不要把“打开过”强行做成用户要维护的状态

打开记录是客观行为，不是强制状态流转。

### 6.6 提醒是用户主动操作，不是系统默认安排

当前产品不做：
- 每日待看摘要
- 自动给所有收藏安排提醒
- 把所有收藏默认归为待看

### 6.7 先小步确认，不一步到位

后面继续开发时，建议按这个节奏：
- 每完成一小段就确认
- 不一次改完所有模块
- 尤其是数据库、提醒、交互这三块，不要一起大改

## 7. 当前一个需要注意的偏差

`supabase/phase1_schema.sql` 里目前还保留了 `collections.status`。

这是因为前期曾讨论过“未看/已看/归档”模型，后来产品方向改成了：

- “待看”来自提醒
- 不在每条收藏上强行展示状态

当前建议：
- 这个字段可以暂时保留在数据库里
- 但第一阶段不要把它做成核心 UI
- 后面如果确认根本不需要，再删掉也可以

## 8. 当前最建议的下一步

如果继续推进，优先顺序建议是：

1. 执行 `supabase/phase1_schema.sql`
2. 新增分类数据层
3. 首页接分类和基础筛选
4. 记录打开时间
5. 做卡片 `...` 菜单
6. 再进入 Telegram 单条提醒
