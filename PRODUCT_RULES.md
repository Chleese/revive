# Revive 产品约定

这份文档记录当前项目已经确定下来的产品规则。  
后续继续开发时，如果代码实现和这里冲突，以这份文档为准。

## 1. 平台和分类分开

- `platform` 是系统客观属性。
- `category` 是用户主观语义。
- 平台用于识别来源、图标、抓取策略。
- 分类用于用户自己整理内容。

举例：
- 平台可以是 `xiaohongshu`
- 分类可以是 `健身`

两者不是一回事，不能混用。

## 2. Telegram 提醒是可选功能

- 用户没有 Telegram，也必须能正常使用收藏功能。
- Telegram 只是增强能力，不是产品门槛。
- 不允许把“是否绑定 Telegram”做成影响基本使用的前提。

## 3. 待看不是所有收藏的默认状态

目前已明确：

- 普通收藏，不自动算“待看”
- 只有用户主动为某条收藏设置了提醒，这条内容当前才算“待看”
- “待看”来自提醒系统，不来自 `collections` 主表的默认状态

因此当前阶段不做“每条内容显示待看/已看”这种 UI。

## 4. 提醒是对单条内容的主动操作

- 提醒入口挂在单条收藏上
- 不是全局每日摘要提醒
- 不是系统自动替用户安排待看队列

第一版提醒模型：

- 用户对某条收藏设置一个具体时间点
- 到时间后 Telegram 提醒用户去看这条内容
- 每条收藏第一版只允许 1 个有效提醒

## 5. 自定义时间是核心能力

- 快捷时间以后可以做
- 但第一版必须支持用户自己选日期和时间
- 提醒字段统一使用：
  - 数据库：`remind_at`
  - 代码：`remindAt`

## 6. metadata 规则放后端，不放前端

- 前端只负责把用户原始输入发出去
- 后端统一判断：
  - 标题
  - `metadata_source`
  - `metadata_confidence`
  - `needs_edit`

不要把 metadata 判断规则分散写在多个页面组件里。

## 7. metadata_source 第一版合法取值

当前第一版约定值：
- `share_text`
- `og`
- `json_ld`
- `title_tag`
- `fallback`
- `manual`

这些不是网站自动给的字段，而是我们自己定义的一组“来源标签”。

## 8. 手动改标题必须记录为 manual

如果用户手动改了标题：
- `title` 更新成用户输入的新标题
- `metadata_source = 'manual'`

因为这个场景现在已经存在，所以 `manual` 必须第一版就纳入，而不是以后再说。

## 9. 打开记录是客观行为，不是强制状态流转

用户可能：
- 收藏后长期不打开
- 某天突然搜索后直接打开
- 看完也不想做任何标记

因此：
- “打开过”应该来自系统记录
- 不是必须让用户手动维护的状态
- 当前阶段优先考虑 `last_opened_at`，后续可扩展 `opened_count` 和浏览记录

## 10. 分类、提醒、浏览行为三者分开

后续始终按这三层理解产品：

- 分类：用户主观整理
- 提醒：用户主动安排某个时间点查看
- 浏览记录：系统客观记录是否打开过、什么时候打开过

不要把这三者混成一个状态系统。

## 11. 当前阶段不做的事情

当前产品不做：
- 每日待看摘要提醒
- 自动给所有收藏安排提醒
- 把所有收藏默认归为待看
- 强制的已看/未看状态 UI

如果以后产品方向变化，要先更新这份文档，再改实现。

## 12. 公开展示层与私有收藏层分离

后期如果增加“个人展示页 / 展示书单 / 对外分享页”，必须遵守以下原则：

- `collections` 是用户私有收藏主库，不等于公开展示内容。
- 公开展示层只能从私有收藏中“挑选部分内容”组成，不能默认公开全部收藏。
- 公开展示层不能直接暴露私有字段，例如：
  - `raw_input`
  - `notes`
  - `metadata_confidence`
  - 提醒信息
  - 打开记录
- 公开展示页的主路由心智采用：
  - 路由：`/u/[slug]`
  - `slug`：用户唯一公开标识
- `slug` 后期必须唯一，适合用于分享链接。
- 如果以后想支持 `@用户名` 风格，优先作为展示别名，而不是主实现路径。
